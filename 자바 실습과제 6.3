public class Main {
  public static void main(String[] args) {
    int a=650,b=3800;
    Factorial fuck = new Factorial();

    int fuc = fuck.fac(a,b);

    System.out.println("GCD " + a+" "+b + " is " + fuc);
  }
}

public class Factorial {
  public int fac(int a,int b) {

    if(a >= b) {
      if(a%b==0)
        return b;
      else
        return fac(b,a%b);
    }
    else {
      if(b%a==0)
        return a;
      else
        return fac(a,b%a);
    }
    
    }
  }
